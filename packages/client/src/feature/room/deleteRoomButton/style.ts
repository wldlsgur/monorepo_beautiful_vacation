import styled from 'styled-components';

export const Button = styled.button`
  position: absolute;
  padding: 0;
  background: none;
  border: none;
  right: 1.5rem;
  bottom: -1.5rem;
  cursor: pointer;
`;
